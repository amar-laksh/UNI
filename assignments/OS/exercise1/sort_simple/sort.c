#include <stdio.h>
int get_input(int fd)
{
    while
	return 0;
}
int main(int argc, char** argv)
{
    int ret = 1;
    int fd;
    if (argc > 1) {
	ret = 2;
    } else {
	ret = get_input(argv);
    }
    return ret;
}
